echo "# sabs-network" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Deevenge/sabs-network.git
git push -u origin main

git remote add origin https://github.com/Deevenge/sabs-network.git
git branch -M main
git push -u origin main

git add .
git commit -m "updated the mobile cards"
git push -u origin main
